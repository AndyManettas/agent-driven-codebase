version: 1

status_values:
  - backlog
  - ready
  - in_progress
  - blocked
  - in_review
  - done

tasks:
  - id: APP-001
    title: Add input validation to POST endpoint
    status: ready
    priority: P1
    size: S
    area: api
    deps: []
    plan: plans/APP-001-input-validation.md
    report: .agents/reports/APP-001.md
    files:
      - src/**
      - tests/**
    acceptance:
      - Invalid input returns 400 with field-level errors.
      - Valid input proceeds normally.
    verify:
      - npm test
      - npm run lint && npm test
