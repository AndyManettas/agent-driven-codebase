# apps/web/AGENTS.md

This directory owns the web frontend application.

## Always
- Follow the component patterns in `examples/`.
- Add accessibility attributes to interactive elements.
- Write component tests for new UI.

## Never
- Import server-side code directly.
- Add global CSS outside the design system.
