# local-agents

Adopt this module in monorepos or large repos that need subtree-specific `AGENTS.md` files.

Keep local agent files additive and delta-only. They should refine the root `AGENTS.md`, not replace it.
