# scripts

Adopt this module only when the repo does not already have a stable command surface.

It provides placeholder wrappers for:
- `bootstrap.sh`
- `lint.sh`
- `typecheck.sh`
- `test.sh`
- `smoke.sh`
- `verify.sh`

Replace each script with the repo's real commands before relying on it.
