# ci

Adopt this module when the repo lacks an existing CI workflow that already calls its verify entrypoint.

The provided GitHub Actions example delegates to the repo's own scripts rather than introducing a second verification contract.
